version: '3'

services:

  mysqldb:
    container_name: mysqldb
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=contact_list_db
    ports:
      - "3306:3306"